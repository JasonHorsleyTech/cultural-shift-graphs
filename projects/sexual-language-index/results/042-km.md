# Result: Ticket 042 — Khmer (`km`)

## Outcome: SUCCESS

- Dump word count: 282
- Enrichment record count: 281
- Coverage: 99.6%

## Notes on data quality

The Khmer dump contains many entries with degraded Khmer script — single
isolated characters (e.g. `ស`, `ហ`, `ខ`) that are not real lexical items
but rather syllable fragments produced by the Pass-1 model. These have
been enriched using the supplied romanizations and category context, with
low `recognition_pct_current` values (15–25%) and `notes` flagging the
quality concern. The well-formed Khmer terms (e.g. `លិង្គ`, `យោនី`, `ខ្ទើយ`,
`ការរួមភេទ`, `ចូទ`, `ពេស្យា`, `ព្រហ្មចរ្យ`, `ស្រីខូច`) are enriched with
proper recognition scores and registry-mapped concepts.

Khmer's culture-specific entries — the `ខ្ទើយ` (kathoey-equivalent) third-
gender system, the Buddhist Third Precept moral framework around sexual
misconduct, and the female honor/shame vocabulary — are mostly mapped as
`is_novel_concept: true` since the registry lacks direct equivalents.
`khteuey` itself is treated as a novel-concept slug rather than mapping
onto English `kathoey` or `transgender_slur`.

Sources: native Khmer linguistic knowledge plus the Sanskrit/Pali roots
of the formal register (`លិង្គ`/`យោនី`/`ពេស្យា`/`ព្រហ្មចរ្យ`).

Confidence: medium-high for the ~80 well-formed lemmas; low for the ~200
single-character/garbled fragments which are nonetheless preserved with
their original romanizations to maintain dump↔enrichment alignment.

<!-- COMPLETE -->
