# Result: Ticket 011 — Modern Standard Arabic (`ar`)

## Outcome: SUCCESS

## Counts

- **Dump word count:** 579 words (`data/dump/ar-words.txt`)
- **Enrichment record count:** 546 records (`data/enriched/ar.jsonl`)
- **Coverage:** 94.3% (above the 80% threshold)

## Script execution

- `dump-language.sh ar`: skipped (dump file already existed from a prior run)
- `enrich-batch.sh ar`: ran 24 batches in parallel (25 words per batch, 579 words total)
- All 24 batches completed with `is_error: false` — no rate-limit (429) errors observed
- Final JSONL written to `data/enriched/ar.jsonl`

## Notes

- PATH fix required: `/opt/homebrew/bin` not in cron shell's PATH; `node` not found at default PATH `/usr/bin:/bin`. Ran script with `PATH="/opt/homebrew/bin:/usr/bin:/bin"` prepended.
- 33 words from the dump did not produce enrichment records (579 − 546 = 33). Likely caused by the model consolidating near-duplicate terms or skipping untranslatable entries within a batch. Within acceptable range.

<!-- COMPLETE -->
